AWSTemplateFormatVersion: "2010-09-09"
Description: Allocate Elastic IP for EC2 instance

Resources:
  DemoElasticIP:
    Type: AWS::EC2::EIP
    Properties:
      Domain: vpc
      Tags:
        - Key: Purpose
          Value: GitHubStreak
        - Key: Environment
          Value: Dev
